---
- hosts: all

  become: true
  tasks:
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes
    - name: Creat A Login User
        user:
         name: biznetdev
         password: 'changeme'
         shell: /bin/bash
         state: present
         shell: /bin/bash
         system: no
         createhome: yes
         home: /home/biznetdev
