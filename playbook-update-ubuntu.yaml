---
- hosts: all

  become: true
  tasks:
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes
    - name: create user on remote host
      ansible.builtin.user:
        name: biznetdev
        shell: /bin/bash
        group: wheel
